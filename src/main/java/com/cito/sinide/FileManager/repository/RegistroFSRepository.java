package com.cito.sinide.FileManager.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.cito.sinide.FileManager.entity.RegistroFS;

public interface RegistroFSRepository extends JpaRepository<RegistroFS, String>{

}
